#
# Variables common to all groups
#
---
user:
  name: Denis Luchkin-Zhou
  email: wyvernzora@gmail.com
  github: wyvernzora

nodejs:
  n_install_path: "{{ ansible_env.HOME }}/.n"
  npm_prefix: "{{ ansible_env.HOME }}/.npm"
  versions:
    - lts
    - stable

zsh:
  dotfile_path: $HOME/.zsh

golang:
  prefix: /usr/local
  gopath: "{{ ansible_env.HOME }}/.go"
