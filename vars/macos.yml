---
git:
  credential_helper: osxkeychain

homebrew_taps: "{{ packages.homebrew.taps }}"
homebrew_cask_apps: "{{ packages.homebrew.casks }}"
homebrew_installed_packages: "{{ packages.homebrew.packages }}"

macos:
  force_defaults: No
